# Context Map

本仓库包含两个领域上下文(bounded context),各自维护一份词汇表:

- **ccusage-go CLI** — `./CONTEXT.md`:本地 JSONL 日志解析、token 聚合与报表输出。
- **Token 排行榜(token-leaderboard)** — `./server/CONTEXT.md`:客户端上报、服务端聚合与排行榜/仪表盘展示。

系统级架构决策记录: `./docs/adr/`
